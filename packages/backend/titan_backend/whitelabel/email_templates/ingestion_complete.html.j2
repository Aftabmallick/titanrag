<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Ingestion Complete — {{ filename }}</title>
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background-color: #f8fafc; margin: 0; padding: 24px; color: #1e293b; }
    .card { max-width: 560px; margin: 0 auto; background: #ffffff; border-radius: 12px; border: 1px solid #e2e8f0; padding: 32px; box-shadow: 0 4px 6px -1px rgba(0,0,0,0.05); }
    .brand-logo { max-height: 44px; margin-bottom: 24px; display: block; }
    .btn { display: inline-block; background-color: {{ primary_color | default('#6366f1') }}; color: #ffffff !important; padding: 12px 24px; border-radius: 8px; text-decoration: none; font-weight: 600; margin-top: 20px; }
    .stats-box { background: #f1f5f9; border-radius: 8px; padding: 16px; margin: 20px 0; }
    .footer { margin-top: 32px; font-size: 12px; color: #94a3b8; text-align: center; }
  </style>
</head>
<body>
  <div class="card">
    {% if logo_url %}
    <img src="{{ logo_url }}" alt="{{ company_name }}" class="brand-logo" />
    {% else %}
    <h2 style="color: {{ primary_color | default('#6366f1') }}; margin-top: 0;">{{ company_name }}</h2>
    {% endif %}

    <h3>Document Ingestion Completed</h3>
    <p>Your document has been parsed, chunked, and indexed into the vector database.</p>
    
    <div class="stats-box">
      <strong>File:</strong> {{ filename }}<br>
      <strong>Chunks generated:</strong> {{ chunk_count }}<br>
      <strong>Tokens indexed:</strong> {{ token_count }}<br>
      <strong>Duration:</strong> {{ duration_seconds }}s
    </div>

    <p><a href="{{ document_url }}" class="btn">View in Workspace</a></p>
    
    <div class="footer">
      Powered by {{ company_name }} &bull; Enterprise RAG Engine
    </div>
  </div>
</body>
</html>
